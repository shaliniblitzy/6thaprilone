"""
Blitzy Platform API Test Suite - Source Module

Core modules providing API client, configuration management,
validation utilities, and response models for testing the
percent_complete field across Blitzy Platform API endpoints.

Submodules:
    config      - Environment-based configuration management (Settings, base URLs, tokens)
    api_client  - HTTP client wrapping requests for the three target API endpoints
    validators  - Validation functions for percent_complete field presence, type, and range
    models      - Pydantic response models defining expected API response schemas
"""
