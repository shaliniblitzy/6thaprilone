# Blitzy Platform API Test Suite — Python Dependencies
#
# All required packages for the automated API response test coverage suite
# that validates the percent_complete field across three Blitzy Platform
# API endpoints: GET /runs/metering, GET /runs/metering/current, and
# GET /project.

# Core test framework
# NOTE: CVE-2025-71176 (CVSS 6.8 MEDIUM) affects pytest through 9.0.2 on UNIX —
# predictable /tmp/pytest-of-{user} paths allow local symlink attacks (DoS / privilege
# escalation).  No patched release is available as of 2026-04.  Risk is LOW for this
# dev-only tool.  Mitigation: set PYTEST_DEBUG_TEMPROOT to a secure, user-private
# directory (e.g. export PYTEST_DEBUG_TEMPROOT="$HOME/.pytest_tmp") or run tests inside
# isolated containers.  Monitor https://github.com/pytest-dev/pytest/issues/13669 for
# an upstream fix.
pytest>=8.3.0

# HTTP client for API requests
requests>=2.32.0

# JSON schema validation for response structure assertions
jsonschema>=4.23.0

# Data validation and response model definitions
pydantic>=2.9.0

# Environment variable loading from .env files
python-dotenv>=1.0.0

# YAML configuration file parsing
pyyaml>=6.0.0

# HTML test report generation
pytest-html>=4.1.0

# Test execution timeout enforcement
pytest-timeout>=2.3.0
